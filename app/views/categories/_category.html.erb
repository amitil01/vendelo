<div id="<%= dom_id category %>">
   <%= category.name %>
   <%= link_to 'Editar', edit_category_path(category) %> 
   <%= link_to 'Eliminar', category_path(category), data: {turbo_method: :delete, turbo_confirm: 'Esta seguro?'} %> 
</div>
